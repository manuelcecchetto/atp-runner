{
  "name": "atp-runner",
  "version": "1.0.0",
  "private": true,
  "description": "Headless ATP Task Runner",
  "type": "module",
  "scripts": {
    "start": "tsx atp_runner.ts",
    "test": "node --import tsx --test atp_runner.test.ts"
  },
  "dependencies": {
    "@openai/codex-sdk": "^0.111.0",
    "tsx": "^4.21.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}
